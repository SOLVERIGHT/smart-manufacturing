resources:
  jobs:
    oee_pipeline:
      name: oee-pipeline

      tasks:
        - task_key: bronze_ingestion
          spark_python_task:
            python_file: ../src/bronze/autoloader_quality_downtime.py
          environment_key: default

      environments:
        - environment_key: default
          spec:
            client: "2"